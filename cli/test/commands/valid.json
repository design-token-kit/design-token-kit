{
  "$schema": "https://www.designtokens.org/schemas/2025.10/format.json",
  "$description": "Shared requisite — minimal valid DTCG JSON input. Used by: validate, check, convert, help. Tests reference this as 'a valid token file' to exercise command logic.",
  "primitive": {
    "color": {
      "white": {
        "$type": "color",
        "$value": {"colorSpace":"srgb","components":[1,1,1],"alpha":1,"hex":"#ffffff"}
      }
    }
  }
}
